<h3>Top 10 najbardziej obserwowanych wierszy:</h3>

<%= render(
        partial: 'poems/list',
        locals: {poems: Poem.most_observed}
    ) %>

<hr>

<h3>Top 10 najbardziej polubianych wersji:</h3>

<% Line.most_liked.map{|line| line.poem_version}.each do |favourite_poem| %>
    <%= render(
            partial: 'poems/version',
            locals: {favourite_poem: favourite_poem}
        ) %>
<% end %>